const express = require('express');
const router = express.Router();

// Admin routes placeholder
router.get('/dashboard', (req, res) => {
  res.json({ message: 'Admin dashboard - implement as needed' });
});

module.exports = router;